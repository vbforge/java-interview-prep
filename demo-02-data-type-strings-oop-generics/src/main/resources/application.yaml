server:
  port: 8082

spring:
  application:
    name: demo-02-data-type-strings-oop-generics

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    com.vbforge: DEBUG
